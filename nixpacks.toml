# Pin Node for Railway / Nixpacks builds
[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
# Force official registry (local lockfiles may have been generated via npmmirror)
cmds = [
  "npm config set registry https://registry.npmjs.org/",
  "npm ci --omit=dev"
]

[start]
cmd = "npm start"
